<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sureshots.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.DashboardMainActivity"
            android:label="Super Shot Discounts"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".activity.PaymentSuccessActivity"
            android:label=""/>

        <activity
            android:name=".activity.SubscriptionActivity"
            android:label="Benefits of Subscription"/>

        <activity
            android:name=".activity.RechargeTwoActivity"
            android:label="Recharge"/>

        <activity
            android:name=".activity.RechargeOneActivity"
            android:label="Recharge"/>

        <activity
            android:name=".activity.ReferEarnActivity"
            android:label="Refer and Earn"/>

        <activity
            android:name=".activity.VerifyOTPActivity"
            android:label=""/>

        <activity
            android:name=".activity.SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity"/>
    </application>

</manifest>